extern "stdlib.o" {
  extend_open(a,b);
  extend_read(a,b);
  extend_close(a);
  print(a,b);
}

[1,1] main(args){
  [1,5] bar := extend_read(5, extend_open("./compile.sh", "r"));
  return bar -> print(1,"Made it this far\n") -> 0;
}
