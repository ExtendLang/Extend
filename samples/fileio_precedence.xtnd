bmi() {
  q1 := fwrite("What is your height in inches?\n", STDOUT);
  height := q1 -> parseFloat(freadline(STDIN));
  q2 := height -> fwrite("What is your weight in pounds?\n", STDOUT);
  weight := q2 -> parseFloat(freadline(STDIN));
  return weight * 703 / height ** 2;
}
